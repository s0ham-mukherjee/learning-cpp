#include <iostream>
#include <string>

int main(){

    std::string name;


    /*
    while(name.empty()){

        std::cout << "Enter your name: ";
        std::getline(std::cin,name);
    }

    std:: cout << "Hello 
    " << name << '\n';
    */

    /*
    do{
        std::cout << "Enter you name: ";
        std::getline(std::cin,name);
    }while (name.empty());
    */

    /*
    for(int i = 0; i < 10 ; i++){
        std::cout << "One day ill work for citadel" << '\n';
    }
    */
    
    return 0;
}